import { ComingSoon } from "@/components/coming-soon";

export default function CoverPage() {
  return (
    <ComingSoon
      title="Cover"
      description="Generate cover artwork, add typography and build the full KDP wraparound cover with calculated spine."
      phase={5}
    />
  );
}
